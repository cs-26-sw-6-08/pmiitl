// always[0s,9s] t = 1 s | t = 2 s | t = 3 s | t = 4 s |t = 5 s | t = 6 s |t = 7 s | t = 8 s;

// always t%2s = 0s | t%2s = 1s;

//Eventually[0s,4s] 0s = t;
//Eventually[0s,4s] 0;
//always[0s,2s] t = 10000000s;

//always counttime(power > 3w) < 4 s;

//always t%4s = 0s -> always[1s,4s](counttime(1) < 6s);

//always count(name) = t /1s;

//always sum(power) = 10w;

//always count(foreach(power > 10w));

//always sum(power) / 2w = 2.5;

always count(foreach(power >= 0w)) > 2  ;